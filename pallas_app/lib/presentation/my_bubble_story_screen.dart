import 'package:flutter/material.dart';

class MyBubbleStoryScreen extends StatelessWidget {
  const MyBubbleStoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stories of my bubble', style: TextStyle(fontSize: 24)),
    );
  }
}
